-- See https://wiki.hypr.land/configuring/core/monitors/
-- Empty output = the fallback rule for every monitor without an explicit one.

hl.monitor({
    output   = "",
    mode     = "preferred",
    position = "auto",
    scale    = "auto",
})
